import React, {FC} from 'react';
import Price from "./Price";

const TicketsOrder: FC = () => {
    return (
        <div className={'tickets__order'}>
            <Price/>
        </div>
    );
};

export default TicketsOrder;
